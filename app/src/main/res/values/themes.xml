<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorSecondary">@color/colorWarning</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="LayoutBackground">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>
    
    <style name="TextContent" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/white</item>
    </style>
    
    <style name="TextTitle" parent="Widget.AppCompat.TextView">
        <item name="fontFamily">@font/brandon_bld</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorSecondary</item>
    </style>

    <style name="CustomText" parent="Widget.AppCompat.TextView">
        <item name="color">@color/colorPrimary</item>
        <item name="fontFamily">@font/brandon_reg</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="BigTitle" parent="CustomText">
        <item name="fontFamily">@font/brandon_bld</item>
        <item name="android:textSize">24sp</item>
    </style>

    <style name="MediumText" parent="CustomText">
        <item name="fontFamily">@font/brandon_med</item>
    </style>

    <style name="PlaintText" parent="CustomText">
    </style>

</resources>
